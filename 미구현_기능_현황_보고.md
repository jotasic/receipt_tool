# 미구현 기능 현황 보고

**작성일**: 2026-02-15
**프로젝트**: receipt_tool
**분석 범위**: 데이터베이스 스키마, UI 화면, 서비스, 타입 정의

---

## 요약

receipt_tool 프로젝트는 **통합 Item 시스템**(personal_card, corporate_card, proof_document)으로 전환된 상태이며, 기본적인 CRUD 기능은 구현되어 있습니다. 그러나 다음과 같은 주요 기능들이 미구현 상태입니다:

- **태그 시스템** (DB 정의 완료, UI/통합 미구현)
- **커스텀 필드** (DB 정의 완료, UI/통합 미구현)
- **카테고리 관리** (DB 정의 완료, 레거시 시스템과 통합 필요)
- **동적 사용 용도 관리** (DB 테이블 존재, CRUD 서비스 미구현)
- **설정 화면 기능** (UI만 존재, 실제 동작 미구현)
- **Item 편집 기능** (TODO 코멘트)
- **Report → Item 마이그레이션** (서비스 구현, UI 미적용)
- **홈 화면 실제 데이터 연동** (하드코딩된 ₩0 표시)

---

## 1. 데이터베이스 & 데이터 모델

### 1.1 태그 시스템 (Tags)

**상태**: DB 완전 구현, UI/통합 미구현

- **DB 테이블**: `tags`, `receipt_tags`, `document_tags`
- **서비스**: `/services/database/tagService.ts` - 완전 구현
  - CRUD 작업 (생성, 읽기, 수정, 삭제, 검색)
  - Receipt 연결 (addTagToReceipt, getTagsForReceipt 등)
  - Document 연결 (addTagToDocument, getTagsForDocument 등)
- **타입**: `/types/tag.ts` - 정의 완료
- **미구현 사항**:
  - UI 컴포넌트 없음 (태그 선택기, 태그 관리 화면)
  - Item 시스템과 통합 필요 (현재 receipt/document 전용)
  - 설정 화면에 태그 관리 메뉴 없음

**파일 위치**:
- `/services/database/tagService.ts` (344 lines)
- `/types/tag.ts`
- `/services/database/schema.ts` (lines 92-119)

**복잡도**: Medium (UI 구현 필요)

---

### 1.2 커스텀 필드 (Custom Fields)

**상태**: DB 완전 구현, UI/통합 미구현

- **DB 테이블**: `custom_fields`, `receipt_custom_values`, `document_custom_values`
- **서비스**: `/services/database/customFieldService.ts` - 완전 구현
  - 4가지 필드 타입 지원 (text, number, date, select)
  - 엔티티 타입별 필터링 (receipt, document, both)
  - 필수 여부 설정, 표시 순서 관리
  - Receipt/Document별 값 저장/조회
- **타입**: `/types/customField.ts` - 정의 완료
- **미구현 사항**:
  - 커스텀 필드 관리 UI (생성, 편집, 삭제)
  - ItemForm에 커스텀 필드 렌더링
  - Item 시스템과 통합 필요
  - 설정 화면에 커스텀 필드 관리 메뉴 없음

**파일 위치**:
- `/services/database/customFieldService.ts` (369 lines)
- `/types/customField.ts`
- `/services/database/schema.ts` (lines 121-154)

**복잡도**: Large (동적 폼 생성, 타입별 렌더링 필요)

---

### 1.3 카테고리 시스템 (Categories)

**상태**: DB/서비스 구현, 레거시 시스템 전용

- **DB 테이블**: `categories`
- **서비스**: `/services/database/categoryService.ts` - 완전 구현
  - CRUD 작업, 사용 통계, 중복 확인
- **현재 상태**:
  - Receipt 테이블에서만 사용 (`receipts.category_id`)
  - 8개 기본 카테고리 시드 완료
- **문제점**:
  - 통합 Item 시스템에서는 `usage_purpose`로 대체됨
  - 레거시 receipt과 신규 items 간 불일치
- **미구현 사항**:
  - 카테고리 관리 UI 없음
  - Item 시스템과의 통합 전략 필요 (마이그레이션 또는 제거)

**파일 위치**:
- `/services/database/categoryService.ts` (219 lines)
- `/services/database/schema.ts` (lines 69-76, 341-350)

**복잡도**: Medium (레거시 시스템 마이그레이션 전략 필요)

---

### 1.4 사용 용도 관리 (Usage Purposes)

**상태**: DB 테이블 존재, CRUD 서비스 미구현

- **DB 테이블**: `usage_purposes` - 정의 완료
  - 필드: id, name, name_en, icon, color, is_active, display_order
- **현재 상태**:
  - 하드코딩된 2개 용도: "meal", "other" (`/constants/items.ts`)
  - DB 초기화 시 DEFAULT_USAGE_PURPOSES 시드됨
- **미구현 사항**:
  - CRUD 서비스 함수 없음 (`usagePurposeService.ts` 미존재)
  - 동적 조회/생성/수정/삭제 불가능
  - 설정 화면에서 사용자가 용도 추가 불가능
  - `UsagePurposeSelector` 컴포넌트가 하드코딩된 상수만 사용

**파일 위치**:
- `/services/database/schema.ts` (lines 184-194, 355-358)
- `/constants/items.ts` (lines 6-23)
- `/components/item/UsagePurposeSelector.tsx`

**복잡도**: Small (CRUD 서비스 구현 후 UI 연동)

---

### 1.5 레거시 테이블 상태

**상태**: 유지 중, 마이그레이션 완료됨

- **레거시 테이블**: `receipts`, `documents`, `report_receipts`, `report_documents`
- **현재 상태**:
  - 통합 마이그레이션 완료 (`migrateToUnifiedModel`)
  - 레거시 데이터 → items 테이블로 복사됨
  - Report 서비스에서 deprecated 함수로 표시
- **문제점**:
  - Store가 여전히 레거시 테이블 사용 (`receiptStore`, `documentStore`)
  - Report 생성 화면(`/report/create.tsx`)이 receipt 참조
  - UI가 완전히 Item 시스템으로 전환되지 않음

**파일 위치**:
- `/store/receiptStore.ts` (54 lines)
- `/store/documentStore.ts` (68 lines)
- `/app/report/create.tsx` (lines 6-12)

**복잡도**: Medium (UI 전환 및 Store 통합)

---

## 2. UI 화면 & 기능

### 2.1 Item 편집 기능

**상태**: TODO 코멘트, 미구현

- **위치**: `/app/item/[id].tsx` (line 130)
- **현재**: "준비 중입니다" Alert 표시
- **미구현 사항**:
  - Item 편집 화면/모달
  - ItemForm의 수정 모드 지원
  - 이미지 교체 기능
  - 수정 내역 저장

**파일 위치**:
- `/app/item/[id].tsx` (line 130)

**복잡도**: Medium (ItemForm 재사용 가능)

---

### 2.2 설정 화면 기능들

**상태**: UI 껍데기만 존재, 실제 동작 미구현

- **위치**: `/app/(tabs)/settings.tsx`
- **미구현 기능**:
  1. **다크 모드** (line 103)
     - Switch 존재하나 상태만 Alert로 표시
     - 실제 테마 전환 로직 없음
  2. **알림 설정** (line 104-109)
     - Alert만 표시
  3. **언어 설정** (line 110-117)
     - "한국어" 하드코딩, 변경 불가
  4. **데이터 백업** (line 126-131)
     - Alert만 표시, 실제 백업 로직 없음
  5. **데이터 복원** (line 132-137)
     - Alert만 표시, 실제 복원 로직 없음
  6. **데이터 초기화** (line 138-154)
     - 확인 다이얼로그만 존재, 실제 삭제 안됨
  7. **앱 정보/도움말** (line 162-175)
     - Alert만 표시

**파일 위치**:
- `/app/(tabs)/settings.tsx` (180 lines)

**복잡도**:
- 다크 모드: Medium (Context API, AsyncStorage)
- 데이터 백업/복원: Large (파일 시스템, 공유 기능)
- 데이터 초기화: Small (DB reset 연동)
- 기타: Small

---

### 2.3 홈 화면 실제 데이터 연동

**상태**: 하드코딩된 UI, 데이터 연동 없음

- **위치**: `/app/(tabs)/index.tsx`
- **현재**:
  - "이번 달 지출: ₩0" (line 33)
  - "증빙 0건" (line 40)
  - "최근 증빙" 빈 상태만 표시 (lines 54-67)
- **미구현 사항**:
  - itemStore에서 데이터 로드
  - 월별 지출 합계 계산
  - 월별 항목 개수 표시
  - 최근 항목 3-5개 표시

**파일 위치**:
- `/app/(tabs)/index.tsx` (94 lines)

**복잡도**: Small (itemStore 연동만 필요)

---

### 2.4 Report 화면 Item 시스템 마이그레이션

**상태**: 서비스 구현 완료, UI 미적용

- **위치**:
  - `/app/report/create.tsx`
  - `/app/report/[id].tsx`
  - `/app/(tabs)/reports.tsx`
- **현재 상태**:
  - `useReceiptStore` 사용 (line 6, create.tsx)
  - Receipt 선택 UI만 존재
  - reportService는 Item 지원 (`getReportItems`, `linkItemToReport`)
- **미구현 사항**:
  - Item 선택 UI로 전환
  - 분류별 필터 (personal_card, corporate_card, proof_document)
  - Report 상세 화면에 items 표시
  - receiptIds/documentIds 제거 후 items 배열로 전환

**파일 위치**:
- `/app/report/create.tsx` (175 lines)
- `/app/report/[id].tsx` (193 lines)
- `/services/database/reportService.ts` (lines 331-507)

**복잡도**: Medium (UI 전환, Store 통합)

---

### 2.5 미구현 UI 컴포넌트

**상태**: 필요하나 존재하지 않음

1. **TagSelector** (태그 선택 컴포넌트)
   - 다중 선택, 자동완성, 새 태그 생성
2. **CustomFieldRenderer** (커스텀 필드 렌더링)
   - 타입별 입력 위젯 (text, number, date, select)
3. **CategoryManager** (카테고리 관리 화면)
   - 생성/수정/삭제, 아이콘/색상 선택
4. **UsagePurposeManager** (사용 용도 관리 화면)
   - 동적 추가/수정/삭제, 정렬
5. **BackupRestoreUI** (백업/복원 화면)
   - 파일 선택, 진행 상황 표시

**복잡도**: Medium ~ Large

---

## 3. 서비스 & 비즈니스 로직

### 3.1 Usage Purpose Service

**상태**: 완전 미구현

- **필요 기능**:
  ```typescript
  // /services/database/usagePurposeService.ts (미존재)
  - getUsagePurposes(): Promise<UsagePurpose[]>
  - getUsagePurposeById(id: string): Promise<UsagePurpose | null>
  - createUsagePurpose(input: CreateUsagePurposeInput): Promise<UsagePurpose>
  - updateUsagePurpose(id: string, updates: UpdateUsagePurposeInput): Promise<void>
  - deleteUsagePurpose(id: string): Promise<void>
  - toggleUsagePurposeActive(id: string, isActive: boolean): Promise<void>
  - reorderUsagePurposes(ids: string[]): Promise<void>
  ```

**파일 위치**: `/services/database/usagePurposeService.ts` (생성 필요)

**복잡도**: Small (기존 서비스 패턴 참고 가능)

---

### 3.2 Tag Integration Service

**상태**: DB 서비스 존재, Item 통합 필요

- **현재**: Receipt/Document만 지원
- **필요 기능**:
  ```typescript
  - addTagToItem(itemId: string, tagId: string): Promise<void>
  - removeTagFromItem(itemId: string, tagId: string): Promise<void>
  - getTagsForItem(itemId: string): Promise<Tag[]>
  - getItemsByTag(tagId: string): Promise<Item[]>
  ```
- **DB 스키마 변경 필요**:
  - `item_tags` 테이블 생성 또는
  - Items 테이블에 JSON 필드 추가

**복잡도**: Medium (스키마 변경, 마이그레이션)

---

### 3.3 Custom Field Integration Service

**상태**: DB 서비스 존재, Item 통합 필요

- **현재**: Receipt/Document만 지원
- **필요 기능**:
  ```typescript
  - getCustomFieldsForItem(entityType: 'item'): Promise<CustomField[]>
  - setItemCustomValue(itemId: string, fieldId: string, value: string): Promise<void>
  - getItemCustomValues(itemId: string): Promise<CustomFieldValue[]>
  ```
- **DB 스키마 변경 필요**:
  - `item_custom_values` 테이블 생성 또는
  - `custom_fields.entity_type`에 'item' 추가

**복잡도**: Medium (스키마 변경, 마이그레이션)

---

### 3.4 Backup/Restore Service

**상태**: 완전 미구현

- **필요 기능**:
  ```typescript
  // /services/backup/index.ts (미존재)
  - exportDatabase(): Promise<string> // JSON/SQLite 파일 경로
  - importDatabase(filePath: string): Promise<void>
  - validateBackupFile(filePath: string): Promise<boolean>
  - getBackupMetadata(filePath: string): Promise<BackupMetadata>
  ```
- **고려사항**:
  - 파일 시스템 권한 (WRITE_EXTERNAL_STORAGE)
  - 이미지 파일 포함 여부
  - 공유 기능 (Share API)

**파일 위치**: `/services/backup/` (디렉토리 생성 필요)

**복잡도**: Large (파일 I/O, 권한, 데이터 검증)

---

### 3.5 Theme/Settings Service

**상태**: 완전 미구현

- **필요 기능**:
  ```typescript
  // /services/settings/index.ts (미존재)
  - getSettings(): Promise<Settings>
  - updateSettings(updates: Partial<Settings>): Promise<void>
  - getTheme(): Promise<'light' | 'dark' | 'system'>
  - setTheme(theme: 'light' | 'dark' | 'system'): Promise<void>
  ```
- **저장소**: AsyncStorage 또는 SQLite settings 테이블

**복잡도**: Small (AsyncStorage 사용 시)

---

## 4. 우선순위별 구현 필요 항목

### P0 (Critical - 기본 기능 동작 필요)

| 항목 | 설명 | 위치 | 복잡도 | 예상 시간 |
|------|------|------|--------|----------|
| **Item 편집 기능** | 항목 수정 불가능 (현재 삭제만 가능) | `/app/item/[id].tsx` | Medium | 4h |
| **홈 화면 데이터 연동** | 하드코딩된 ₩0 표시 | `/app/(tabs)/index.tsx` | Small | 2h |
| **Usage Purpose 서비스** | 동적 용도 관리 불가능 | `/services/database/usagePurposeService.ts` | Small | 3h |
| **데이터 초기화 실제 동작** | 설정 화면 기능 실제 구현 | `/app/(tabs)/settings.tsx` | Small | 1h |

**합계**: ~10시간

---

### P1 (High - 사용자 경험 개선)

| 항목 | 설명 | 위치 | 복잡도 | 예상 시간 |
|------|------|------|--------|----------|
| **Report → Item 마이그레이션** | Report 화면이 레거시 Receipt 사용 | `/app/report/` | Medium | 6h |
| **Usage Purpose 관리 UI** | 설정에서 용도 추가/수정/삭제 | 신규 화면 | Medium | 4h |
| **다크 모드 지원** | 테마 전환 기능 | 전체 앱 | Medium | 8h |
| **Tag 시스템 UI** | 태그 선택/관리 화면 | 신규 컴포넌트 | Medium | 6h |

**합계**: ~24시간

---

### P2 (Medium - 추가 기능)

| 항목 | 설명 | 위치 | 복잡도 | 예상 시간 |
|------|------|------|--------|----------|
| **커스텀 필드 시스템** | 동적 필드 추가/렌더링 | 신규 컴포넌트 | Large | 12h |
| **백업/복원 기능** | 데이터 내보내기/가져오기 | `/services/backup/` | Large | 10h |
| **카테고리 관리 UI** | 카테고리 추가/수정 (또는 제거) | 신규 화면 | Medium | 4h |
| **Tag → Item 통합** | Item에 태그 추가 기능 | DB 스키마 변경 | Medium | 6h |

**합계**: ~32시간

---

### P3 (Low - Nice to have)

| 항목 | 설명 | 위치 | 복잡도 | 예상 시간 |
|------|------|------|--------|----------|
| **알림 설정** | 제출 기한 알림 등 | `/services/notification/` | Medium | 6h |
| **언어 설정** | 다국어 지원 (i18n) | 전체 앱 | Large | 16h |
| **Custom Field → Item 통합** | Item에 커스텀 필드 추가 | DB 스키마 변경 | Medium | 6h |
| **도움말/튜토리얼** | 첫 사용자 가이드 | 신규 화면 | Medium | 8h |
| **레거시 Store 제거** | receiptStore, documentStore 완전 삭제 | `/store/` | Small | 4h |

**합계**: ~40시간

---

## 5. 기술 부채 & 개선 사항

### 5.1 레거시 시스템 정리

**문제**: Receipt/Document 시스템과 Item 시스템 공존

- **영향받는 파일**:
  - `/store/receiptStore.ts`
  - `/store/documentStore.ts`
  - `/app/report/create.tsx`
  - `/app/report/[id].tsx`
  - `/services/database/reportService.ts` (deprecated 함수들)

**권장 조치**:
1. Report UI를 완전히 Item 기반으로 전환
2. receiptStore, documentStore 제거
3. reportService에서 deprecated 함수 제거
4. 레거시 테이블 유지 여부 결정 (마이그레이션 완료 후 제거 가능)

---

### 5.2 타입 일관성

**문제**: UsagePurpose 타입 정의가 여러 곳에 산재

- **위치**:
  - `/types/item.ts`: `UsagePurpose = 'meal' | 'other'`
  - `/types/shared.ts`: `UsagePurposeConfig` 인터페이스
  - `/services/database/schema.ts`: `UsagePurposeRow` 인터페이스
  - `/constants/items.ts`: 하드코딩된 배열

**권장 조치**:
- 단일 타입 정의 파일로 통합
- DB에서 동적 조회하도록 변경

---

### 5.3 서비스 인덱스 불완전

**문제**: `/services/database/index.ts`에 Item 서비스 미포함

- **현재**: itemService 함수들이 export 안됨
- **해결**: index.ts에 추가 필요

---

## 6. 완료된 기능 (참고)

다음 기능들은 정상적으로 구현되어 있습니다:

✅ **Item CRUD** - 생성, 읽기, 삭제 (편집 제외)
✅ **Item 분류 시스템** - personal_card, corporate_card, proof_document
✅ **OCR 통합** - ItemForm에서 이미지 텍스트 추출
✅ **Calendar 통합** - 날짜별 Item 표시
✅ **File 시스템 관리** - 이미지 저장/삭제
✅ **Report 기본 기능** - 생성, 조회, 제출, 상태 관리
✅ **Database 마이그레이션** - 레거시 → 통합 모델
✅ **Tag 서비스** - DB 레벨 완전 구현
✅ **Custom Field 서비스** - DB 레벨 완전 구현
✅ **Category 서비스** - DB 레벨 완전 구현

---

## 7. 다음 단계 권장 사항

### 단계 1: P0 항목 완료 (1-2주)
1. Item 편집 기능 구현
2. 홈 화면 실제 데이터 연동
3. Usage Purpose CRUD 서비스 생성
4. 데이터 초기화 실제 동작

### 단계 2: 레거시 시스템 정리 (1주)
1. Report UI → Item 기반 전환
2. receiptStore, documentStore 제거
3. deprecated 함수 제거

### 단계 3: P1 항목 선택 구현 (2-3주)
1. Usage Purpose 관리 UI (사용자가 용도 추가 가능)
2. Tag 시스템 UI
3. 다크 모드 (선택적)

### 단계 4: P2/P3 항목 선택 구현 (필요시)
1. 커스텀 필드 시스템
2. 백업/복원
3. 기타 부가 기능

---

## 부록: 파일 구조 요약

```
receipt_tool/
├── app/
│   ├── (tabs)/
│   │   ├── index.tsx           # ❌ 데이터 연동 필요
│   │   ├── items.tsx           # ✅ 완료
│   │   ├── calendar.tsx        # ✅ 완료
│   │   ├── reports.tsx         # ⚠️  레거시 사용
│   │   └── settings.tsx        # ❌ 껍데기만
│   ├── item/
│   │   ├── add.tsx             # ✅ 완료
│   │   └── [id].tsx            # ⚠️  편집 미구현
│   └── report/
│       ├── create.tsx          # ⚠️  레거시 사용
│       └── [id].tsx            # ⚠️  레거시 사용
├── services/
│   └── database/
│       ├── itemService.ts      # ✅ 완료
│       ├── reportService.ts    # ✅ 완료 (Item 지원)
│       ├── tagService.ts       # ✅ 완료 (UI 미연동)
│       ├── customFieldService.ts # ✅ 완료 (UI 미연동)
│       ├── categoryService.ts  # ✅ 완료 (레거시)
│       └── usagePurposeService.ts # ❌ 미존재
├── store/
│   ├── itemStore.ts            # ✅ 완료
│   ├── receiptStore.ts         # ⚠️  제거 예정
│   └── documentStore.ts        # ⚠️  제거 예정
├── components/
│   ├── item/
│   │   ├── ItemForm.tsx        # ✅ 완료
│   │   └── ItemCard.tsx        # ✅ 완료
│   └── [미구현 컴포넌트들]
│       ├── TagSelector         # ❌ 미존재
│       ├── CustomFieldRenderer # ❌ 미존재
│       └── UsagePurposeManager # ❌ 미존재
└── types/
    ├── item.ts                 # ✅ 완료
    ├── tag.ts                  # ✅ 완료
    ├── customField.ts          # ✅ 완료
    └── category.ts             # ✅ 완료
```

**범례**:
- ✅ 완전 구현
- ⚠️  부분 구현 또는 레거시 사용
- ❌ 미구현

---

## 총 예상 작업 시간

| 우선순위 | 작업 시간 |
|---------|----------|
| P0 (필수) | ~10시간 |
| P1 (높음) | ~24시간 |
| P2 (중간) | ~32시간 |
| P3 (낮음) | ~40시간 |
| **총합** | **~106시간** |

**실제 개발 시간**: 테스트, 디버깅, 문서화 포함 시 **약 150-200시간** 예상

---

**보고서 끝**
